<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portofolio | David Pangihutan Hutapea</title>
    <!-- Menggunakan Font Google (Inter) -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
    <!-- Menggunakan FontAwesome untuk Ikon -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* === VARIABEL CSS & RESET === */
        :root {
            --bg-color: #0b1120;
            --surface-color: #1e293b;
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
            --primary: #38bdf8;
            --primary-hover: #0284c7;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            line-height: 1.6;
            overflow-x: hidden;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        /* === NAVIGASI === */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 1.5rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(11, 17, 32, 0.8);
            backdrop-filter: blur(10px);
            z-index: 1000;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--text-main);
        }

        .logo span {
            color: var(--primary);
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 2rem;
        }

        nav ul li a {
            font-weight: 600;
            font-size: 0.9rem;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: var(--primary);
        }

        /* === BAGIAN HERO (BERANDA) === */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 0 5%;
            padding-top: 80px;
        }

        .hero-content {
            max-width: 800px;
        }

        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 4.5rem);
            font-weight: 800;
            margin-bottom: 1rem;
            line-height: 1.2;
        }

        .typing-container {
            font-size: clamp(1.2rem, 3vw, 2rem);
            color: var(--primary);
            font-weight: 600;
            margin-bottom: 1.5rem;
            min-height: 40px;
        }

        .typing-text {
            border-right: 3px solid var(--primary);
            padding-right: 5px;
            animation: blink 0.75s step-end infinite;
        }

        @keyframes blink {
            from, to { border-color: transparent; }
            50% { border-color: var(--primary); }
        }

        .hero p {
            color: var(--text-muted);
            font-size: 1.1rem;
            margin-bottom: 2rem;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .btn {
            display: inline-block;
            padding: 0.8rem 2rem;
            background-color: var(--primary);
            color: #0b1120;
            font-weight: 600;
            border-radius: 8px;
            transition: all 0.3s ease;
            border: 2px solid var(--primary);
        }

        .btn:hover {
            background-color: transparent;
            color: var(--primary);
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(56, 189, 248, 0.2);
        }

        .btn-outline {
            background-color: transparent;
            color: var(--text-main);
            border-color: var(--text-muted);
            margin-left: 1rem;
        }

        .btn-outline:hover {
            border-color: var(--text-main);
            color: var(--text-main);
            box-shadow: none;
        }

        /* === BAGIAN GLOBAL === */
        section {
            padding: 5rem 5%;
        }

        .section-title {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 3rem;
            text-align: center;
        }

        .section-title span {
            color: var(--primary);
        }

        /* === BAGIAN PROYEK === */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2rem;
        }

        .project-card {
            background-color: var(--surface-color);
            border-radius: 16px;
            padding: 2rem;
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s ease;
        }

        .project-card:hover {
            transform: translateY(-10px);
            border-color: rgba(56, 189, 248, 0.3);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        }

        .project-icon {
            font-size: 2.5rem;
            color: var(--primary);
            margin-bottom: 1.5rem;
        }

        .project-card h3 {
            font-size: 1.4rem;
            margin-bottom: 1rem;
        }

        .project-card p {
            color: var(--text-muted);
            margin-bottom: 1.5rem;
            font-size: 0.95rem;
        }

        .tags {
            display: flex;
            gap: 0.5rem;
            margin-bottom: 1.5rem;
            flex-wrap: wrap;
        }

        .tag {
            background-color: rgba(56, 189, 248, 0.1);
            color: var(--primary);
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .project-links a {
            font-size: 0.9rem;
            font-weight: 600;
            color: var(--text-main);
            margin-right: 1.5rem;
            transition: color 0.3s ease;
        }

        .project-links a:hover {
            color: var(--primary);
        }

        /* === BAGIAN KONTAK === */
        .contact {
            text-align: center;
            max-width: 600px;
            margin: 0 auto;
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .social-links a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            background-color: var(--surface-color);
            border-radius: 50%;
            font-size: 1.2rem;
            transition: all 0.3s ease;
        }

        .social-links a:hover {
            background-color: var(--primary);
            color: #0b1120;
            transform: translateY(-5px);
        }

        /* === FOOTER === */
        footer {
            text-align: center;
            padding: 2rem;
            color: var(--text-muted);
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            font-size: 0.9rem;
        }

        /* === KELAS UNTUK ANIMASI JS === */
        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s ease;
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* === RESPONSIVITAS === */
        @media (max-width: 768px) {
            nav ul {
                display: none; /* Disembunyikan untuk versi mobile yang simpel */
            }
            .btn-outline {
                margin-left: 0;
                margin-top: 1rem;
            }
            .hero-buttons {
                display: flex;
                flex-direction: column;
                gap: 1rem;
            }
        }
    </style>
</head>
<body>

    <!-- Navigasi -->
    <header>
        <div class="logo">David<span>.</span></div>
        <nav>
            <ul>
                <li><a href="#beranda">Home</a></li>
                <li><a href="#proyek">Projects</a></li>
                <li><a href="#kontak">Contact</a></li>
            </ul>
        </nav>
    </header>

    <!-- Halaman Utama -->
    <section id="beranda" class="hero">
        <div class="hero-content reveal active">
            <h1>Halo! Saya <br><span>David Pangihutan Hutapea</span></h1>
            
            <div class="typing-container">
                Saya seorang <span class="typing-text"></span>
            </div>

            <p>Siswa kelas XII TJKT 1 di SMK N 2 Kota Jambi. Saya sangat antusias dengan dunia infrastruktur jaringan dan pengembangan web. Berbekal pengalaman praktik lab hingga terjun langsung ke lapangan, saya selalu siap belajar dan menghadapi tantangan baru di dunia teknologi.</p>
            
            <div class="hero-buttons">
                <a href="#proyek" class="btn">Lihat Dokumentasi</a>
                <a href="https://github.com/Davidhutapea" target="_blank" class="btn btn-outline"><i class="fab fa-github"></i> Kunjungi GitHub</a>
            </div>
        </div>
    </section>

    <!-- Halaman Proyek -->
    <section id="proyek">
        <h2 class="section-title reveal">Dokumentasi <span>Projek</span></h2>
        
        <div class="projects-grid">
            <!-- Kartu Proyek 1 -->
            <div class="project-card reveal">
                <i class="fa-solid fa-network-wired project-icon"></i>
                <h3>Desain Topologi Cisco</h3>
                <p>Merancang dan menyimulasikan arsitektur jaringan skala menengah menggunakan Cisco Packet Tracer. Proyek ini mematangkan pemahaman saya tentang routing, switching, dan struktur jaringan yang efisien.</p>
                <div class="tags">
                    <span class="tag">Cisco</span>
                    <span class="tag">Routing</span>
                    <span class="tag">Switching</span>
                </div>
                <div class="project-links">
                    <a href="https://github.com/Davidhutapea" target="_blank"><i class="fab fa-github"></i> Detail Projek</a>
                </div>
            </div>

            <!-- Kartu Proyek 2 -->
            <div class="project-card reveal">
                <i class="fa-solid fa-laptop-code project-icon"></i>
                <h3>Pengembangan Aplikasi Web</h3>
                <p>Membangun berbagai platform digital mulai dari website E-commerce, sistem Marketplace, hingga Aplikasi Web interaktif. Fokus pada fungsionalitas, performa, dan alur pengguna yang nyaman.</p>
                <div class="tags">
                    <span class="tag">Web App</span>
                    <span class="tag">E-Commerce</span>
                    <span class="tag">Marketplace</span>
                </div>
                <div class="project-links">
                    <a href="https://github.com/Davidhutapea" target="_blank"><i class="fab fa-github"></i> Detail Projek</a>
                </div>
            </div>

            <!-- Kartu Proyek 3 -->
            <div class="project-card reveal">
                <i class="fa-solid fa-tools project-icon"></i>
                <h3>Instalasi Jaringan (Biznet)</h3>
                <p>Pengalaman terjun langsung ke lapangan saat magang. Terlibat aktif bersama tim teknisi senior dalam instalasi, perbaikan, merancang topologi fisik, serta memastikan prosedur keselamatan (K3) selalu diterapkan dengan baik.</p>
                <div class="tags">
                    <span class="tag">Biznet</span>
                    <span class="tag">Maintenance</span>
                    <span class="tag">Fiber Optic</span>
                </div>
                <div class="project-links">
                    <a href="https://github.com/Davidhutapea" target="_blank"><i class="fab fa-github"></i> Detail Projek</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Halaman Kontak -->
    <section id="kontak" class="contact reveal">
        <h2 class="section-title">Mari <span>Terhubung</span></h2>
        <p style="color: var(--text-muted); margin-bottom: 2rem;">Tertarik dengan portofolio saya atau ada peluang kolaborasi? Jangan ragu untuk menghubungi saya melalui kontak di bawah ini. Saya selalu terbuka untuk diskusi baru!</p>
        
        <a href="mailto:ytmastervisualty@gmail.com" class="btn"><i class="fa-solid fa-envelope"></i> Kirim Pesan</a>
        
        <div class="social-links">
            <a href="https://github.com/Davidhutapea" target="_blank" title="GitHub"><i class="fab fa-github"></i></a>
            <a href="https://wa.me/6289652078949" target="_blank" title="WhatsApp"><i class="fab fa-whatsapp"></i></a>
            <a href="https://instagram.com/davpht_" target="_blank" title="Instagram"><i class="fab fa-instagram"></i></a>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <p>&copy; 2026 David Pangihutan Hutapea. Dibangun dengan HTML, CSS & JavaScript.</p>
    </footer>

    <!-- === KODE JAVASCRIPT === -->
    <script>
        // 1. Logika Efek Mengetik (Typing Effect)
        const typedTextSpan = document.querySelector(".typing-text");
        const textArray = ["Siswa XII TJKT 1", "Teknisi Jaringan", "Web Developer", "IT Enthusiast"];
        const typingDelay = 100;
        const erasingDelay = 50;
        const newTextDelay = 2000; // Waktu jeda sebelum menghapus
        let textArrayIndex = 0;
        let charIndex = 0;

        function type() {
            if (charIndex < textArray[textArrayIndex].length) {
                typedTextSpan.textContent += textArray[textArrayIndex].charAt(charIndex);
                charIndex++;
                setTimeout(type, typingDelay);
            } else {
                setTimeout(erase, newTextDelay);
            }
        }

        function erase() {
            if (charIndex > 0) {
                typedTextSpan.textContent = textArray[textArrayIndex].substring(0, charIndex - 1);
                charIndex--;
                setTimeout(erase, erasingDelay);
            } else {
                textArrayIndex++;
                if (textArrayIndex >= textArray.length) textArrayIndex = 0;
                setTimeout(type, typingDelay + 1100);
            }
        }

        // Mulai efek mengetik setelah halaman dimuat
        document.addEventListener("DOMContentLoaded", function() {
            if(textArray.length) setTimeout(type, newTextDelay + 250);
        });

        // 2. Logika Animasi Muncul saat di-Scroll (Scroll Reveal)
        function reveal() {
            var reveals = document.querySelectorAll(".reveal");
            for (var i = 0; i < reveals.length; i++) {
                var windowHeight = window.innerHeight;
                var elementTop = reveals[i].getBoundingClientRect().top;
                var elementVisible = 150; // Jarak dari bawah layar sebelum elemen muncul

                if (elementTop < windowHeight - elementVisible) {
                    reveals[i].classList.add("active");
                }
            }
        }

        // Panggil fungsi saat menggulir halaman
        window.addEventListener("scroll", reveal);
        // Panggil sekali di awal untuk memuat elemen yang sudah terlihat
        reveal();
    </script>

</body>
</html>
